export async function getVaultSummary(userId: string) {
  // Stripe + Firestore logic here
}

export async function getTransactionLedger(userId: string) {
  // Firestore query for steward’s transactions
}